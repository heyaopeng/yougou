<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0, user-scalable=no">
	<meta name="keywords" content="Cookabuy, Cooka, Bán buôn quần áo, Bán buôn sản phẩm từ Trung Quốc" />
	<meta name="description" content="Bán buôn quần áo, túi giày chất lượng giá rẻ nhập từ Trung Quốc trên mạng Cookabuy.com" />
	<meta name="baidu-site-verification" content="Sxrec3nsaL" />
	<title>Cookabuy.com: Bán buôn quần áo trên mạng Cookabuy</title>
	<link rel="shortcut icon" href="favicon.ico">
	<link rel="stylesheet" href="./CKF/build/index.css?v={{vnum}}">
	<style>
		#b {
			position: fixed;
			width: 100%;
			height: 100%;
			background-color: #2d435a;
			z-index: 99999;
		}

		#b > img {
			display: block;
			position: absolute;
			max-width: 100%;
			max-height: 100%;
			top: 0;
			left: 0;
			right: 0;
			bottom: 0;
			margin: auto;
		}
	</style>
	<script>
		function setCookie(sKey, sValue, vEnd, sPath, sDomain, bSecure) {
			if (!sKey || /^(?:expires|max\-age|path|domain|secure)$/i.test(sKey)) {
				return false;
			}
			var sExpires = "";
			if (vEnd) {
				switch (vEnd.constructor) {
					case Number:
						sExpires = vEnd === Infinity ? "; expires=Fri, 31 Dec 9999 23:59:59 GMT" : "; max-age=" + vEnd;
						break;
					case String:
						sExpires = "; expires=" + vEnd;
						break;
					case Date:
						sExpires = "; expires=" + vEnd.toUTCString();
						break;
				}
			}
			// sPath should always set to '/';
			sPath = '/';
			document.cookie = encodeURIComponent(sKey) + "=" + encodeURIComponent(sValue) + sExpires + (sDomain ? "; domain=" + sDomain : "") + (sPath ? "; path=" + sPath : "") + (bSecure ? "; secure" : "");
			return true;
		}

		setCookie('language', 'zh_CN', Infinity);
	</script>
</head>
<body>
	<style>
		/* theme */
		.main {
			background-color: white;
		}
	</style>
	<div id="b" style="display:none">
	</div>
	<script>
		var w = Math.max(document.documentElement.clientWidth, window.innerWidth || 0)
		if (w < 800) {
			document.getElementById('b').style.display = 'block';
			var img = new Image();
			img.onload = function () {
				console.log('stop')
				document.getElementById('b').appendChild(img);
				if (window.stop !== undefined) {
					window.stop();
				} else if (document.execCommand !== undefined) {
					document.execCommand("Stop", false);
				}
			}
			img.src = '/CKF/images/building.vi_VN.jpg';
		}
	</script>
	{{> header}}

	<div class="main" id="main">
		<div class="container">

			{{> misc}}
			{{> channel}}
			{{> f1}}
			{{> f2}}
			{{> f3}}
			{{> f4}}
			{{> f5}}
			{{> f6}}
			{{> f7}}

		</div>
	</div>
	
	{{> footer}}

	<script src="./CKF/build/vi_VN.index.js?v={{vnum}}"></script>
</body>
</html>