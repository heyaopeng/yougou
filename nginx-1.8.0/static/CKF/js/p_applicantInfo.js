var CKF = require('./CKF.js');
require('../less/applicant-info.less');

module.exports = (function () {
    // var moduleName = 'applicantInfo';
    // var module = CKF.create(moduleName);

    return {
        init: function () {
            // module.each(function (index, elem) {

            // });
        }
    };
})();