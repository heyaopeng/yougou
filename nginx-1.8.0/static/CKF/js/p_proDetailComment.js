var CKF = require('./CKF.js');

require('../less/product-detail-comment.less');

module.exports = (function () {
    var moduleName = 'proDetailComment';
    var module = CKF.create(moduleName);

    return {
        init: function () {
        	
        }
    };
})();